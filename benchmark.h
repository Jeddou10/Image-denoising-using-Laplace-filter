void implementation(long testCycles, const uint8_t* img, size_t width, size_t height, float a, float b, float c, uint8_t* tmp1, uint8_t* tmp2, uint8_t* result, 
					void implement(const uint8_t* , size_t ,size_t ,float ,float ,float ,uint8_t* ,uint8_t* ,uint8_t*));
void bench_implementation(
        long testCycles, 
        const uint8_t* img,
        size_t width,
        size_t height,
        float a,
        float b,
        float c,
        uint8_t* result,
        int version
        );